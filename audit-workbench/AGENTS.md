# 维护约定

先读 README.md。此目录是公开、通用的工程演示版，不能加入真实台账、内部规范、应用编号、私有接口、凭据或业务截图。

保留七模块、稳定编号、schemaVersion=2、阶段历史、冻结周报和 Excel 逐项选版。团队自选不是权限，归档只允许已完成项目，累计挽损包含归档。业务语义变更须用户明确要求。

默认 local-reference；platform 未配置时必须明确报错，禁止自动回退本机。平台适配限定在 lib/audit 及接口边界；真实目标骨架与认证由目标项目负责，不在此模拟已接通。

验证 npm test、npm run build、npm run typecheck。仅提交源码、必要使用文档和虚构 JSON；生成的 Excel、截图、输出、缓存、数据库均不入库。保留第三方许可。
